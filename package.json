{
  "name": "octez.connect-docs",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "npm run create-node-modules && npm run embed-code && docusaurus start",
    "create-node-modules": "rm -rf src/node_modules && mkdir -p src/node_modules/@tezos-x && cp -r node_modules/@tezos-x src/node_modules/ && mkdir -p src/node_modules/@taquito && cp -r node_modules/@taquito src/node_modules/",
    "prepare": "node scripts/fix-octez.connect-sdk.js",
    "build": "npm run create-node-modules && npm run embed-code && docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "prettier": "npx prettier --write './sidebars.js' 'docusaurus.config.js' && npm run pretty-source",
    "pretty-source": "npx prettier --ignore-unknown --write 'src/**/*'",
    "pretty-docs": "npx prettier --ignore-unknown --write 'docs/**/*'",
    "generate-monaco-types": "node scripts/generate-monaco-types.js",
    "remove-folders": "rm -rf build-docs && rm -rf docs",
    "clear-folders": "npm run remove-folders && mkdir build-docs && mkdir docs",
    "embed-code": "npm run generate-monaco-types && npm run prettier && tsc --module es2015 --target es2015 --moduleResolution node --esModuleInterop true --allowJs true --skipLibCheck true src/examples/*.ts src/types/*.d.ts && node scripts/copy-examples.js && npm run clear-folders && cp -r src/docs/* build-docs/ && rm -r docs && mv build-docs docs && npm run pretty-docs"
  },
  "dependencies": {
    "@docusaurus/core": "^3.9.1",
    "@docusaurus/plugin-client-redirects": "^3.9.1",
    "@docusaurus/preset-classic": "^3.9.1",
    "@docusaurus/theme-live-codeblock": "^3.9.1",
    "@mdx-js/react": "^3.0.1",
    "@monaco-editor/react": "^4.6.0",
    "@tezos-x/octez.connect-sdk": "1.0.0",
    "@taquito/taquito": "24.1.0-beta.0",
    "@taquito/beacon-wallet": "24.1.0-beta.0",
    "@taquito/rpc": "24.1.0-beta.0",
    "@taquito/contracts-library": "24.1.0-beta.0",
    "clsx": "^2.1.1",
    "dompurify": "^3.2.4",
    "mermaid": "^10.9.5",
    "prism-react-renderer": "^2.3.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.9.1",
    "@tsconfig/docusaurus": "^2.0.3",
    "classnames": "^2.5.1",
    "docusaurus-node-polyfills": "^1.0.0",
    "monaco-editor-webpack-plugin": "^7.1.0",
    "typescript": "^5.5.4"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=22"
  }
}
